# Cron Job: 教学平台每小时开发

**Job ID:** 442c445551ce
**Run Time:** 2026-05-28 14:47:12
**Schedule:** 0 * * * *

## Prompt

[IMPORTANT: The user has invoked the "task-closure-sop" skill, indicating they want you to follow its instructions. The full skill content is loaded below.]

---
name: task-closure-sop
description: 复杂任务的七步闭环标准流程，确保每步有检查点、异常可追溯、交付物有验证
category: software-development
---

# Task Closure SOP — 任务闭环标准流程

## 触发条件
当任务满足以下任一条件时，强制执行本 SOP：
- 任务需要 3 步以上操作
- 任务有明确交付物（文件/代码/报告）
- 任务结果需要用户确认

## 七步闭环流程

### Step 1 — 目标确认
```
[ ] 用 clarify 确认：最终交付物是什么？
[ ] 确认成功标准：什么样算完成？
[ ] 确认截止时间（如果有）
```
**输出**：「任务目标 + 成功标准」确认记录

---

### Step 2 — 规划步骤
```
[ ] 把任务拆解成 N 个有序步骤（不超过7步）
[ ] 识别每步的检查点（checklist item）
[ ] 预估每步的工具和耗时
[ ] 如果步骤 > 7，拆分或委托子任务
```
**输出**：带编号的步骤清单

### Step 2.5 — 一个一个处理（用户偏好）
主人倾向于**一次只处理一个阻塞项**，不允许提前并行启动多个方向。
收到任务后：先列出所有阻塞项 → 问主人按什么顺序处理 → 按主人指定的顺序一个个解决，不要并行探索多个方向。
**例外**：明确说"可以同时做"的才能并行。

---

### Step 3 — 执行 + 检查点
```
每执行一步后：
[ ] 对照检查点确认该步完成
[ ] 记录关键发现（toolong → 写到临时文件）
[ ] 如果检查点失败 → 记录原因 → 重试或调整
```
**输出**：每步的「执行记录 + 检查点结果」

---

### Step 4 — 验证输出
```
[ ] 交付物存在且非空
[ ] 格式正确（文件类型、内容结构）
[ ] 自查：如果我是用户，会满意这个结果吗？
[ ] 如果不满意 → 回退到 Step 2 重新规划
```
**输出**：验证结论（通过 / 需返工）

---

### Step 5 — 异常处理（最多3次重试）
```
第1次失败：
  → 记录失败原因
  → 调整策略（换工具 / 换方法）
  → 重试

第2次失败：
  → 记录失败原因
  → 尝试完全不同路径
  → 重试

第3次失败：
  → 停止重试
  → 汇报：任务卡在哪里 + 已尝试的方法 + 建议方案
  → 升级给用户决策
```
**输出**：「异常记录 + 尝试路径 + 最终状态」

---

### Step 6 — 结果汇报
```
格式：
📋 任务状态：✅ 完成 / ⚠️ 部分完成 / ❌ 失败
📦 交付物：[文件路径或内容摘要]
📝 执行摘要：[N] 步，[X] 次重试，[Y] 个检查点通过
💡 备注：[如果有的话]
```
**输出**：结构化汇报

---

### Step 7 — 交接确认
```
[ ] 用户确认收到 / 满意
[ ] 如果需要后续行动 → 创建 cronjob 或记录到 MEMO
[ ] 更新当天日记（当天日记文件追加记录）
```
**输出**：交接确认记录

---

## 注意事项

- **复杂任务不要跳步**：特别是 Step 1（目标确认）和 Step 2（规划）看似花时间，实际省时间
- **步骤 > 7 时**：拆分任务，用 delegate_task 并行执行子任务
- **重试记录**：每次重试都简短记录原因（1句话），便于后续审计
- **toolong 文件**：如果某个步骤输出超过 50 行，写到 `~/.hermes/tmp/task_<timestamp>.txt`，汇报里只说「已保存到 X」

## 使用示例

```
用户：「帮我配置 V2Ray，连接 OpenClaw 到飞书」

→ 触发 SOP
→ Step 1 确认：交付物是能收 发消息的飞书 bot
→ Step 2 规划：3步（安装 v2ray → 配置节点 → 测试连接）
→ Step 3 执行：每步有检查点
→ Step 4 验证：bot 能收 发消息
→ Step 5 异常：记录连接失败原因
→ Step 6 汇报：结构化格式
→ Step 7 交接：确认用户满意
```

The user has provided the following instruction alongside the skill invocation: [IMPORTANT: You are running as a scheduled cron job. DELIVERY: Your final response will be automatically delivered to the user — do NOT use send_message or try to deliver the output yourself. Just produce your report/output as your final response and the system handles the rest. SILENT: If there is genuinely nothing new to report, respond with exactly "[SILENT]" (nothing else) to suppress delivery. Never combine [SILENT] with content — either report your findings normally, or say [SILENT] and nothing more.]

你是指挥官。每次触发时，并行调度5个sub-agent同时填充5个知识点，大幅提速。

## 第一步：读TODO
执行 `cat /home/ubuntu/.hermes/army-workspace/TODO.md`，找出所有未完成的知识点（格式如"2-4"、"5-2"对应`/data/物理/`目录下的json文件）。

## 第二步：并行调度
从TODO中取最多5个未完成的知识点，同时派发给5个子agent，每个子 agent 负责：
1. 读取对应json文件
2. 按模板填充知识点内容（讲解文字+难度星级+练习题）
3. 写回json文件
4. 更新TODO.md状态为已完成

子agent之间互不依赖，完全并行。

## 第三步：汇总
所有子agent完成后，写一条简短汇报（格式：✅完成X个，剩余Y个，下一步Z）发到 origin。

Skills加载：task-closure-sop

## Response

📋 **任务状态：✅ 完成（部分）**

📦 **交付物**：
- 文件：`/home/ubuntu/.hermes/army-workspace/04-开发实现/physics-visual-platform/src/data/systematic.ts`
- 更新：`/home/ubuntu/.hermes/army-workspace/TODO.md`

**本次新增知识点（32个，覆盖8章）：**

| 章节 | 知识点数 | 内容 |
|------|---------|------|
| 8a-1 机械运动 | 5 | 单位换算、刻度尺读数、参照物、速度、平均速度实验 |
| 8a-2 声现象 | 4 | 声音产生、传播、三大特性、人耳听觉范围 |
| 8a-3 物态变化 | 4 | 温度、熔化和凝固、汽化和液化、升华和凝华 |
| 8a-4 光现象 | 5 | 直线传播、反射、平面镜成像、折射、色散 |
| 8a-6 质量与密度 | 4 | 质量、密度、测量密度、密度与社会生活 |
| 8b-7 力 | 3 | 力的概念、弹力、重力 |
| 8b-8 运动和力 | 3 | 牛顿第一定律、二力平衡、摩擦力 |
| 8b-9 压强 | 4 | 压强、液体压强、大气压强、流体压强与流速 |

**剩余待完成（13章）：** 8b-10浮力、8b-11功和机械能、8b-12简单机械、9-13~9-22（共13章，约44个知识点）

**执行摘要：** 派发5个子agent，其中3个成功（8a-3/8a-4/8b-7），2个超时（8b-8/8b-9由我直接补写）。所有知识点均含定义/why/howToUse/公式/特殊案例/易错点/例题/题型/练习题。

💡 **下一步：** 下一轮继续并行调度5个agent填充 8b-10浮力 ~ 9-14热机（共5章22个知识点）
