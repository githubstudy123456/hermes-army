# Cron Job: 每3天检测Trojan主订阅可用性

**Job ID:** b0a5e8acfde7
**Run Time:** 2026-05-07 21:41:19
**Schedule:** every 4320m

## Prompt

[IMPORTANT: You are running as a scheduled cron job. DELIVERY: Your final response will be automatically delivered to the user — do NOT use send_message or try to deliver the output yourself. Just produce your report/output as your final response and the system handles the rest. SILENT: If there is genuinely nothing new to report, respond with exactly "[SILENT]" (nothing else) to suppress delivery. Never combine [SILENT] with content — either report your findings normally, or say [SILENT] and nothing more.]

测试主订阅 Trojan 节点（zz.xg01.fogvip-zz.uk:40001）是否能访问 GitHub 和国外主要网站。

测试方法：
1. 用 Xray 生成临时 Trojan 配置（UUID: 1d63b9b8-f166-413d-9c73-fdac64fd819e，SNI: ld.xgg01.yuiuingkig.icu，allowInsecure=0）
2. 启动临时 xray 进程
3. curl --max-time 10 --proxy http://127.0.0.1:10809 https://github.com -o /dev/null -w "%{http_code}"
4. 同时测 Google、YouTube、npmjs
5. 如果全部返回 2xx/3xx 状态码 → 切换 /etc/xray/config.json 到 Trojan outbound，重启 xray service
6. 如果有任何失败 → 保持当前 VMess WebSocket 配置不变，任务结束

切换配置方法：
- 备份当前 /etc/xray/config.json 到 /etc/xray/config.json.vmess.bak
- 写入新的 Trojan 配置文件到 /etc/xray/config.json
- systemctl restart xray
- 验证 GitHub 是否通

测试完成后将结果输出（是否切换成功，当前使用什么协议/节点）。

## Response

Trojan 节点测试结果：

| 站点 | 状态码 |
|------|--------|
| GitHub | 200 ✅ |
| Google | 301 ✅ |
| YouTube | 301 ✅ |
| npmjs | 301 ✅ |

**所有测试通过。** Trojan 节点 zz.xg01.fogvip-zz.uk:40001 可以正常访问 GitHub 及国外主要网站。

**切换结果：失败（需要人工审批）**

执行 `sudo tee /etc/xray/config.json` 和 `systemctl restart xray` 被系统审批流程拦截，无法在无人值守的 cron 环境中自动完成系统文件修改和服务重启。

**当前状态：** 仍使用原 VMess WebSocket 配置（planb.mojcn.com），配置切换需人工操作。

**人工切换步骤（需审批后执行）：**
```bash
sudo cp /etc/xray/config.json /etc/xray/config.json.vmess.bak
sudo tee /etc/xray/config.json > /dev/null << 'EOF'
# [使用上方 Trojan 配置]
EOF
sudo systemctl restart xray
# 验证: curl --proxy http://127.0.0.1:10809 https://github.com -o /dev/null -w "%{http_code}"
```

**回滚命令：** `sudo cp /etc/xray/config.json.vmess.bak /etc/xray/config.json && sudo systemctl restart xray`
