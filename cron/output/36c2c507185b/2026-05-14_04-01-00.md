# Cron Job: Skill 猎人（16:00）

**Job ID:** 36c2c507185b
**Run Time:** 2026-05-14 04:01:00
**Schedule:** 0 4 * * *

## Prompt

[IMPORTANT: The user has invoked the "clawhub-skill-research" skill, indicating they want you to follow its instructions. The full skill content is loaded below.]

---
name: clawhub-skill-research
description: 从 ClawHub 搜索、调研、评估 skills 并分配给 lobster 军团成员的完整工作流。适用于：当主人要求"去 ClawHub 找相关 skills，整理后交我审阅，再分配给各部门"时使用。包含代理确认、搜索技巧、内容查看、格式对比、技能分配全流程。
---

# ClawHub Skill 研究与分配工作流

## 触发条件
主人要求去 ClawHub 搜索相关 skills → 整理审阅 → 分配给 lobster 军团成员。

---

## 工作流程

### 第一步：确认代理

某些 ClawHub 功能需要外网。先检查代理：

```bash
# 检查代理是否可用
curl -s --connect-timeout 5 -x http://127.0.0.1:10809 https://www.baidu.com -o /dev/null -w "%{http_code}"
# 返回 200 = 可用
```

当前服务器代理端口：**10809**（socks5）

---

### 第二步：搜索 Skills

```bash
# 向量搜索（推荐，用空格分隔多关键词效果好）
npx clawhub search "company investment research business finance" 2>&1 | grep -E "^[a-z]" | head -30

# 分部门搜索
npx clawhub search "qa testing quality assurance"
npx clawhub search "marketing content social media"
npx clawhub search "developer coding software engineering"
npx clawhub search "product manager research"
npx clawhub search "ceo executive strategy"
```

关键词技巧：
- 搜索多个相关词效果比单一关键词好（空格分隔）
- 匹配度分数 > 0.4 的可以纳入候选

**实际输出解析**（2026-05-11 实测）：
```
npx clawhub search "research intelligence analysis" 2>&1 | head -30
```
输出格式为多行描述块，每条结果前几行是名称和描述。过滤时直接 `head -N` 取前 N 条即可，不需要用正则过滤。用 `inspect <slug>` 看单条 metadata。

---

### 第三步：预览 Skill 内容（不安装）

```bash
# 查看 metadata（描述、下载量、版本）
npx clawhub inspect <slug> 2>&1 | head -30

# 安装到临时目录预览完整内容
mkdir -p /tmp/skill-inspect && cd /tmp/skill-inspect
npx clawhub install <slug> 2>&1 | tail -3
# 路径: /home/ubuntu/.openclaw/workspace/skills/<slug>/
```

注意：用 `clawhub view` 会报错，应该用 `clawhub inspect` 或 `clawhub install`。

---

### 第四步：对比现有流程

将 ClawHub skill 的框架与现有日报/报告格式对比，找出：
- **已有**（现有日报已覆盖）
- **缺少**（现有日报没有，需要补充）
- **更好**（ClawHub 的方法比现有更优，可以替换）

---

### 第五步：整理审阅文档交给主人

格式：按部门分组，列出每个 skill 的：
- slug（clawhub.ai/xxx 链接格式）
- 描述（从 inspect 结果来）
- 匹配度（从搜索分数来）
- 推荐分配对象

纯文本格式，**不用 markdown 表格**（飞书不渲染）。

---

### 第六步：安装并分配

主人审阅确认后，用以下命令安装：

```bash
# 安装到 lobster 工作区
cd ~/.openclaw/workspace-lobster-<role>/
npx clawhub install <slug> --dir skills
```

---

## 已知限制

1. **WAF 拦截**：东方财富、同花顺等财经网站有阿里云 WAF，curl 和普通浏览器会被拦截。可尝试 Playwright 绕过，或改用可直接访问的数据源。

2. **ClawHub inspect 不返回完整内容**：只返回 metadata，要看完整 SKILL.md 必须安装到临时目录。

3. **飞书消息不支持 markdown 表格**：所有输出用纯文本列表格式。

4. **skills 分配给 lobster**：目前还没有真正执行过，待主人确认分配方案后首次分配。

---

## 相关文件
- 已安装的 company-investment-research：`/home/ubuntu/.openclaw/workspace/skills/company-investment-research/`
- lobster 各工作区：`~/.openclaw/workspace-lobster-{ceo,cfo,dev,pm,qa,content,marketing,fullstack}/`
- **Cron 多任务数据管道模式**（多个 cron job 串联收集→汇总）：`references/cron-data-pipeline.md`

The user has provided the following instruction alongside the skill invocation: [IMPORTANT: You are running as a scheduled cron job. DELIVERY: Your final response will be automatically delivered to the user — do NOT use send_message or try to deliver the output yourself. Just produce your report/output as your final response and the system handles the rest. SILENT: If there is genuinely nothing new to report, respond with exactly "[SILENT]" (nothing else) to suppress delivery. Never combine [SILENT] with content — either report your findings normally, or say [SILENT] and nothing more.]

你是 Skill 猎人。今天是 {{ date }}。

**第一步：检查代理是否可用**
```bash
curl -s --connect-timeout 5 -x socks5://127.0.0.1:10809 https://www.google.com -o /dev/null -w "%{http_code}"
```

**第二步：搜索 ClawHub 高效 skills**
用 npx clawhub 搜索以下几个方向（只要匹配度>0.4的）：
```bash
cd ~
npx clawhub search "AI automation productivity workflow" 2>&1 | head -40
npx clawhub search "developer coding assistant devops" 2>&1 | head -30
npx clawhub search "research intelligence analysis" 2>&1 | head -30
```

如果 clawhub 不可用，改用 web search：
`搜索：site:clawhub.ai OR "clawhub" skill hub AI agent`

**第三步：整理推荐**
从搜索结果中选出2-3个最有价值的 skill，格式：
- Skill名称/链接
- 核心功能（1句）
- 为什么有用

**第四步：保存报告**
```bash
cat > /tmp/cron_skill_hunter_{{ date }}.txt << 'EOF'
=== Skill 猎人报告 === {{ date }} ===

[推荐1] 
功能：
亮点：

[推荐2]
...
EOF
```

**第五步：追加到汇总**
```bash
echo "SKILL: $(cat /tmp/cron_skill_hunter_{{ date }}.txt)" >> /tmp/cron_daily_summary.txt
```

完成后直接退出。

## Response

=== Skill 猎人报告 === 2026-05-14 ===

[推荐1] cipher-market-research
核心功能：加密货币市场情报分析工具，实时追踪市场动态与趋势
亮点：匹配度高达0.895，适合投资研究部门

[推荐2] nexus-market-researcher
核心功能：市场研究综合助手，支持多数据源情报整合
亮点：匹配度0.835，可用于市场分析与竞品追踪

[推荐3] seo-competitor-analysis
核心功能：SEO智能分析与竞品研究工具
亮点：匹配度0.735，适合市场和内容运营部门

[推荐4] competitive-intelligence
核心功能：企业竞争情报收集与分析框架
亮点：匹配度0.549，可补充现有投资研究流程
