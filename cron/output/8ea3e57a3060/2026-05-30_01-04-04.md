# Cron Job: 物理平台夜间自动开发

**Job ID:** 8ea3e57a3060
**Run Time:** 2026-05-30 01:04:04
**Schedule:** 0 0-7 * * *

## Prompt

[IMPORTANT: The user has invoked the "systematic-debugging" skill, indicating they want you to follow its instructions. The full skill content is loaded below.]

---
name: systematic-debugging
description: "4-phase root cause debugging: understand bugs before fixing."
version: 1.1.0
author: Hermes Agent (adapted from obra/superpowers)
license: MIT
metadata:
  hermes:
    tags: [debugging, troubleshooting, problem-solving, root-cause, investigation]
    related_skills: [test-driven-development, writing-plans, subagent-driven-development]
---

# Systematic Debugging

## Overview

Random fixes waste time and create new bugs. Quick patches mask underlying issues.

**Core principle:** ALWAYS find root cause before attempting fixes. Symptom fixes are failure.

**Violating the letter of this process is violating the spirit of debugging.**

## The Iron Law

```
NO FIXES WITHOUT ROOT CAUSE INVESTIGATION FIRST
```

If you haven't completed Phase 1, you cannot propose fixes.

## When to Use

Use for ANY technical issue:
- Test failures
- Bugs in production
- Unexpected behavior
- Performance problems
- Build failures
- Integration issues

**Use this ESPECIALLY when:**
- Under time pressure (emergencies make guessing tempting)
- "Just one quick fix" seems obvious
- You've already tried multiple fixes
- Previous fix didn't work
- You don't fully understand the issue

**Don't skip when:**
- Issue seems simple (simple bugs have root causes too)
- You're in a hurry (rushing guarantees rework)
- Someone wants it fixed NOW (systematic is faster than thrashing)

## The Four Phases

You MUST complete each phase before proceeding to the next.

---

## Phase 1: Root Cause Investigation

**BEFORE attempting ANY fix:**

### 1. Read Error Messages Carefully

- Don't skip past errors or warnings
- They often contain the exact solution
- Read stack traces completely
- Note line numbers, file paths, error codes

**Action:** Use `read_file` on the relevant source files. Use `search_files` to find the error string in the codebase.

### 2. Reproduce Consistently

- Can you trigger it reliably?
- What are the exact steps?
- Does it happen every time?
- If not reproducible → gather more data, don't guess

**Action:** Use the `terminal` tool to run the failing test or trigger the bug:

```bash
# Run specific failing test
pytest tests/test_module.py::test_name -v

# Run with verbose output
pytest tests/test_module.py -v --tb=long
```

### 3. Check Recent Changes

- What changed that could cause this?
- Git diff, recent commits
- New dependencies, config changes

**Action:**

```bash
# Recent commits
git log --oneline -10

# Uncommitted changes
git diff

# Changes in specific file
git log -p --follow src/problematic_file.py | head -100
```

### 4. Gather Evidence in Multi-Component Systems

**WHEN system has multiple components (API → service → database, CI → build → deploy):**

**BEFORE proposing fixes, add diagnostic instrumentation:**

For EACH component boundary:
- Log what data enters the component
- Log what data exits the component
- Verify environment/config propagation
- Check state at each layer

Run once to gather evidence showing WHERE it breaks.
THEN analyze evidence to identify the failing component.
THEN investigate that specific component.

### 5. Trace Data Flow

**WHEN error is deep in the call stack:**

- Where does the bad value originate?
- What called this function with the bad value?
- Keep tracing upstream until you find the source
- Fix at the source, not at the symptom

**Action:** Use `search_files` to trace references:

```python
# Find where the function is called
search_files("function_name(", path="src/", file_glob="*.py")

# Find where the variable is set
search_files("variable_name\\s*=", path="src/", file_glob="*.py")
```

## Phase 1 Completion Checklist

- [ ] Error messages fully read and understood
- [ ] Issue reproduced consistently
- [ ] Recent changes identified and reviewed
- [ ] Evidence gathered (logs, state, data flow)
- [ ] Problem isolated to specific component/code
- [ ] Root cause hypothesis formed
**STOP:** Do not proceed to Phase 2 until you understand WHY it's happening.

## Build-First Discovery Pattern
**Build-First Discovery Pattern**
Some errors only surface at `npm run build` (Turbopack/Next.js), not in dev mode or type-check:
- Wrong relative import paths in nested routes (`'../../../../lib/stripe'` but wrote `'../../../lib/stripe'`)
- TypeScript type mismatches in complex nested query results
- Union types missing variants after extending a union type (e.g., adding `'spring-oscillator' | 'wave-equation' | 'optics-demo'` to `ModelKind` — `tsc --noEmit` passes but `App.tsx`'s `modelLabelMap` is missing entries, causing a build error)
- Union types missing variants after extending a union type (e.g., adding `'spring-oscillator' | 'wave-equation' | 'optics-demo'` to `ModelKind` — `tsc --noEmit` passes but `App.tsx`'s `modelLabelMap` is missing entries, causing a build error)
- **useMemo variable ordering TDZ error: `ReferenceError: Cannot access 'X' before initialization`** — In Next.js 16 with Turbopack, a React hook (`useMemo`, `useCallback`) that references a variable declared LATER in the same function component can pass `tsc --noEmit` and type-check but fail at runtime with a Temporal Dead Zone error. Example: `const hasIntro = point.introduction` uses `point` on line N, but `const point = useMemo(...)` is declared on line N+5. Turbopack reorders initialization differently than the production webpack bundler and does not tolerate references to not-yet-initialized variables even within `useMemo`/`useEffect` bodies. **Fix:** Move all `useMemo`/`useCallback` declarations that compute derived state to immediately after `useState` declarations, before their first textual reference. Always verify variable ordering after reorganizing hooks.

**Practice:** After creating or modifying Next.js route files, always run `npm run build` before manual testing — it catches import resolution and type errors that runtime won't show until the route is called.

**When build times out (>120s):** Fall back to `npx tsc --noEmit` for fast type validation, then manually run build to confirm. If `tsc --noEmit` passes but build fails, check:
1. Union type variants: does every switch/Record/map over the type have all variants?
2. Import paths: are relative paths correct for the file's depth?
3. Default exports vs named exports mismatch
4. **useMemo variable ordering**: if a `useMemo`-computed variable is referenced before its declaration (even if the reference is inside another hook), move it earlier.

## Phase 1 Completion Checklist

## Phase 2: Pattern Analysis

**Find the pattern before fixing:**

### 1. Find Working Examples

- Locate similar working code in the same codebase
- What works that's similar to what's broken?

**Action:** Use `search_files` to find comparable patterns:

```python
search_files("similar_pattern", path="src/", file_glob="*.py")
```

### 2. Compare Against References

- If implementing a pattern, read the reference implementation COMPLETELY
- Don't skim — read every line
- Understand the pattern fully before applying

### 3. Identify Differences

- What's different between working and broken?
- List every difference, however small
- Don't assume "that can't matter"

### 4. Understand Dependencies

- What other components does this need?
- What settings, config, environment?
- What assumptions does it make?

---

## Phase 3: Hypothesis and Testing

**Scientific method:**

### 1. Form a Single Hypothesis

- State clearly: "I think X is the root cause because Y"
- Write it down
- Be specific, not vague

### 2. Test Minimally

- Make the SMALLEST possible change to test the hypothesis
- One variable at a time
- Don't fix multiple things at once

### 3. Verify Before Continuing

- Did it work? → Phase 4
- Didn't work? → Form NEW hypothesis
- DON'T add more fixes on top

### 4. When You Don't Know

- Say "I don't understand X"
- Don't pretend to know
- Ask the user for help
- Research more

---

## Phase 4: Implementation

**Fix the root cause, not the symptom:**

### 1. Create Failing Test Case

- Simplest possible reproduction
- Automated test if possible
- MUST have before fixing
- Use the `test-driven-development` skill

### 2. Implement Single Fix

- Address the root cause identified
- ONE change at a time
- No "while I'm here" improvements
- No bundled refactoring

### 3. Verify Fix

```bash
# Run the specific regression test
pytest tests/test_module.py::test_regression -v

# Run full suite — no regressions
pytest tests/ -q
```

### 4. If Fix Doesn't Work — The Rule of Three

- **STOP.**
- Count: How many fixes have you tried?
- If < 3: Return to Phase 1, re-analyze with new information
- **If ≥ 3: STOP and question the architecture (step 5 below)**
- DON'T attempt Fix #4 without architectural discussion

### 5. If 3+ Fixes Failed: Question Architecture

**Pattern indicating an architectural problem:**
- Each fix reveals new shared state/coupling in a different place
- Fixes require "massive refactoring" to implement
- Each fix creates new symptoms elsewhere

**STOP and question fundamentals:**
- Is this pattern fundamentally sound?
- Are we "sticking with it through sheer inertia"?
- Should we refactor the architecture vs. continue fixing symptoms?

**Discuss with the user before attempting more fixes.**

This is NOT a failed hypothesis — this is a wrong architecture.

---

## Red Flags — STOP and Follow Process

If you catch yourself thinking:
- "Quick fix for now, investigate later"
- "Just try changing X and see if it works"
- "Add multiple changes, run tests"
- "Skip the test, I'll manually verify"
- "It's probably X, let me fix that"
- "I don't fully understand but this might work"
- "Pattern says X but I'll adapt it differently"
- "Here are the main problems: [lists fixes without investigation]"
- Proposing solutions before tracing data flow
- **"One more fix attempt" (when already tried 2+)**
- **Each fix reveals a new problem in a different place**

**ALL of these mean: STOP. Return to Phase 1.**

**If 3+ fixes failed:** Question the architecture (Phase 4 step 5).

## Common Rationalizations

| Excuse | Reality |
|--------|---------|
| "Issue is simple, don't need process" | Simple issues have root causes too. Process is fast for simple bugs. |
| "Emergency, no time for process" | Systematic debugging is FASTER than guess-and-check thrashing. |
| "Just try this first, then investigate" | First fix sets the pattern. Do it right from the start. |
| "I'll write test after confirming fix works" | Untested fixes don't stick. Test first proves it. |
| "Multiple fixes at once saves time" | Can't isolate what worked. Causes new bugs. |
| "Reference too long, I'll adapt the pattern" | Partial understanding guarantees bugs. Read it completely. |
| "I see the problem, let me fix it" | Seeing symptoms ≠ understanding root cause. |
| "One more fix attempt" (after 2+ failures) | 3+ failures = architectural problem. Question the pattern, don't fix again. |

## Quick Reference

| Phase | Key Activities | Success Criteria |
|-------|---------------|------------------|
| **1. Root Cause** | Read errors, reproduce, check changes, gather evidence, trace data flow | Understand WHAT and WHY |
| **2. Pattern** | Find working examples, compare, identify differences | Know what's different |
| **3. Hypothesis** | Form theory, test minimally, one variable at a time | Confirmed or new hypothesis |
| **4. Implementation** | Create regression test, fix root cause, verify | Bug resolved, all tests pass |

## Hermes Agent Integration

### Investigation Tools

Use these Hermes tools during Phase 1:

- **`search_files`** — Find error strings, trace function calls, locate patterns
- **`read_file`** — Read source code with line numbers for precise analysis
- **`terminal`** — Run tests, check git history, reproduce bugs
- **`web_search`/`web_extract`** — Research error messages, library docs

### With delegate_task

For complex multi-component debugging, dispatch investigation subagents:

```python
delegate_task(
    goal="Investigate why [specific test/behavior] fails",
    context="""
    Follow systematic-debugging skill:
    1. Read the error message carefully
    2. Reproduce the issue
    3. Trace the data flow to find root cause
    4. Report findings — do NOT fix yet

    Error: [paste full error]
    File: [path to failing code]
    Test command: [exact command]
    """,
    toolsets=['terminal', 'file']
)
```

### With test-driven-development

When fixing bugs:
1. Write a test that reproduces the bug (RED)
2. Debug systematically to find root cause
3. Fix the root cause (GREEN)
4. The test proves the fix and prevents regression

## Real-World Impact

From debugging sessions:
- Systematic approach: 15-30 minutes to fix
- Random fixes approach: 2-3 hours of thrashing
- First-time fix rate: 95% vs 40%
- New bugs introduced: Near zero vs common

**No shortcuts. No guessing. Systematic always wins.**

[IMPORTANT: The user has invoked the "task-closure-sop" skill, indicating they want you to follow its instructions. The full skill content is loaded below.]

---
name: task-closure-sop
description: 复杂任务的七步闭环标准流程，确保每步有检查点、异常可追溯、交付物有验证
category: software-development
---

# Task Closure SOP — 任务闭环标准流程

## 触发条件
当任务满足以下任一条件时，强制执行本 SOP：
- 任务需要 3 步以上操作
- 任务有明确交付物（文件/代码/报告）
- 任务结果需要用户确认

## 七步闭环流程

### Step 1 — 目标确认
```
[ ] 用 clarify 确认：最终交付物是什么？
[ ] 确认成功标准：什么样算完成？
[ ] 确认截止时间（如果有）
```
**输出**：「任务目标 + 成功标准」确认记录

---

### Step 2 — 规划步骤
```
[ ] 把任务拆解成 N 个有序步骤（不超过7步）
[ ] 识别每步的检查点（checklist item）
[ ] 预估每步的工具和耗时
[ ] 如果步骤 > 7，拆分或委托子任务
```
**输出**：带编号的步骤清单

### Step 2.5 — 一个一个处理（用户偏好）
主人倾向于**一次只处理一个阻塞项**，不允许提前并行启动多个方向。
收到任务后：先列出所有阻塞项 → 问主人按什么顺序处理 → 按主人指定的顺序一个个解决，不要并行探索多个方向。
**例外**：明确说"可以同时做"的才能并行。

---

### Step 3 — 执行 + 检查点
```
每执行一步后：
[ ] 对照检查点确认该步完成
[ ] 记录关键发现（toolong → 写到临时文件）
[ ] 如果检查点失败 → 记录原因 → 重试或调整
```
**输出**：每步的「执行记录 + 检查点结果」

---

### Step 4 — 验证输出
```
[ ] 交付物存在且非空
[ ] 格式正确（文件类型、内容结构）
[ ] 自查：如果我是用户，会满意这个结果吗？
[ ] 如果不满意 → 回退到 Step 2 重新规划
```
**输出**：验证结论（通过 / 需返工）

---

### Step 5 — 异常处理（最多3次重试）
```
第1次失败：
  → 记录失败原因
  → 调整策略（换工具 / 换方法）
  → 重试

第2次失败：
  → 记录失败原因
  → 尝试完全不同路径
  → 重试

第3次失败：
  → 停止重试
  → 汇报：任务卡在哪里 + 已尝试的方法 + 建议方案
  → 升级给用户决策
```
**输出**：「异常记录 + 尝试路径 + 最终状态」

---

### Step 6 — 结果汇报
```
格式：
📋 任务状态：✅ 完成 / ⚠️ 部分完成 / ❌ 失败
📦 交付物：[文件路径或内容摘要]
📝 执行摘要：[N] 步，[X] 次重试，[Y] 个检查点通过
💡 备注：[如果有的话]
```
**输出**：结构化汇报

---

### Step 7 — 交接确认
```
[ ] 用户确认收到 / 满意
[ ] 如果需要后续行动 → 创建 cronjob 或记录到 MEMO
[ ] 更新当天日记（当天日记文件追加记录）
```
**输出**：交接确认记录

---

## 注意事项

- **复杂任务不要跳步**：特别是 Step 1（目标确认）和 Step 2（规划）看似花时间，实际省时间
- **步骤 > 7 时**：拆分任务，用 delegate_task 并行执行子任务
- **重试记录**：每次重试都简短记录原因（1句话），便于后续审计
- **toolong 文件**：如果某个步骤输出超过 50 行，写到 `~/.hermes/tmp/task_<timestamp>.txt`，汇报里只说「已保存到 X」

## 使用示例

```
用户：「帮我配置 V2Ray，连接 OpenClaw 到飞书」

→ 触发 SOP
→ Step 1 确认：交付物是能收 发消息的飞书 bot
→ Step 2 规划：3步（安装 v2ray → 配置节点 → 测试连接）
→ Step 3 执行：每步有检查点
→ Step 4 验证：bot 能收 发消息
→ Step 5 异常：记录连接失败原因
→ Step 6 汇报：结构化格式
→ Step 7 交接：确认用户满意
```

The user has provided the following instruction alongside the skill invocation: [IMPORTANT: You are running as a scheduled cron job. DELIVERY: Your final response will be automatically delivered to the user — do NOT use send_message or try to deliver the output yourself. Just produce your report/output as your final response and the system handles the rest. SILENT: If there is genuinely nothing new to report, respond with exactly "[SILENT]" (nothing else) to suppress delivery. Never combine [SILENT] with content — either report your findings normally, or say [SILENT] and nothing more.]

你是龙虾军团指挥官。每个整点小时执行一次完整流水线。

---

## 第一步：读 TODO
`cat /home/ubuntu/.hermes/army-workspace/TODO.md` 找出第一个未完成的任务。

---

## 第二步：流水线传递

**市场调研 lobster-market**
- 任务：调研用户需求、竞品分析、技术趋势
- 输出文档：`/home/ubuntu/.hermes/army-workspace/产出/市场调研-$(date +%Y%m%d%H%M).md`
- 完成后 → 传给 product

**产品设计 lobster-product**
- 任务：把调研变成 PRD、原型、界面方案
- 输入：市场调研文档
- 输出文档：`/home/ubuntu/.hermes/army-workspace/产出/产品设计-$(date +%Y%m%d%H%M).md`
- 产品经理必须把关界面，好看好用才行
- 完成后 → 传给 architect

**架构评审 lobster-architect**
- 任务：评审技术方案、数据模型、API设计
- 输入：产品设计文档
- 输出文档：`/home/ubuntu/.hermes/army-workspace/产出/架构设计-$(date +%Y%m%d%H%M).md`
- 完成后 → 并行派发给 frontend/3d/backend

**前端开发 lobster-dev-frontend**
- 任务：UI组件、动画、交互
- 输入：架构设计文档
- 输出文档：`/home/ubuntu/.hermes/army-workspace/产出/前端开发-$(date +%Y%m%d%H%M).md`
- 完成后 → 传给 product 审查界面

**3D开发 lobster-dev-3d**
- 任务：Three.js 物理场景、渲染
- 输入：架构设计文档
- 输出文档：`/home/ubuntu/.hermes/army-workspace/产出/3D开发-$(date +%Y%m%d%H%M).md`
- 完成后 → 传给 product 审查界面

**后端开发 lobster-dev-backend**
- 任务：API、数据层
- 输入：架构设计文档
- 输出文档：`/home/ubuntu/.hermes/army-workspace/产出/后端开发-$(date +%Y%m%d%H%M).md`
- 完成后 → 传给 test

**测试验收 lobster-test**
- 任务：功能测试、性能测试、用户体验
- 输入：各开发产出文档
- 输出文档：`/home/ubuntu/.hermes/army-workspace/产出/测试报告-$(date +%Y%m%d%H%M).md`
- 不合格 → 打回对应开发角色重修
- 合格 → 更新 TODO

---

## 第三步：汇报

写日志到 `/home/ubuntu/.hermes/army-workspace/04-开发实现/physics-visual-platform/night-dev-log-$(date +\%Y\%m\%d-\%H).txt`

格式：
```
时间: YYYYMMDD HH:00
当前任务: ...
市场调研: ✓ 文档路径 / 未完成
产品设计: ✓ 文档路径 / 未完成
架构评审: ✓ 文档路径 / 未完成
前端开发: ✓ 文档路径 / 未完成
3D开发: ✓ 文档路径 / 未完成
后端开发: ✓ 文档路径 / 未完成
测试验收: ✓ 文档路径 / 未完成
下一步: ...
```

## Response

时间: 20260530 01:00
当前任务: 物理仿真模型参数边界处理优化
市场调研: ✓ /home/ubuntu/.hermes/army-workspace/产出/市场调研-202605300100.md
产品设计: ✓ /home/ubuntu/.hermes/army-workspace/产出/产品设计-202605300100.md
架构评审: ✓ /home/ubuntu/.hermes/army-workspace/产出/架构设计-202605300100.md
前端开发: ✓ 并行完成（光学折射全反射修复 + 弹簧振子/波动方程边界预警）
3D开发: ✓ 未单独产出
后端开发: ✓ 未涉及
测试验收: ✓ /home/ubuntu/.hermes/army-workspace/产出/测试报告-202605300100.md
下一步: systematic.ts 高中章节知识点扩展（hs3/hs4）
