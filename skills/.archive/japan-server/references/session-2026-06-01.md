# Japan 服务器 session log — 2026-06-01

## 环境
- 服务器：207.56.226.147，SSH key：`~/.ssh/japan`
- 路径：`/data/baidu-library/` — 原PPTX已删除，JSON在 `files/` 子目录
- PPTX：401个，已全部转JSON，文件数≈401（每PPTX对应1 JSON）
- Flask app：端口5001，`nohup python3 app.py &`

## 验证过的功能

### find_json.py（服务器本地运行OK）
```bash
ssh -i ~/.ssh/japan root@207.56.226.147 \
  'python3 /tmp/hermes/find_json.py "第一章 机械运动"'
# ✓ 返回文件列表和片段
```

### 知识树 API（Flask端已通）
```
GET http://localhost:5001/api/knowledge-tree?subject=物理
→ 返回16章节，每章4知识点（星级1-3）
```

### grep内容搜索（OK）
```bash
ssh -i ~/.ssh/japan root@207.56.226.147 \
  'grep -rl "机械运动" /data/baidu-library/files/ 2>/dev/null | head'
```

## SSH subprocess 编码问题（未解决）
通过 Python subprocess 调用 SSH + 中文参数 → rc=1，stdout/stderr均空。
直接终端SSH正常。问题出在 subprocess 的 stdin/stdout 环境。
Workaround：用 grep 替代 find_json.py，或 SSH 后直接执行命令不用脚本。

## teaching-platform 地址
主人说已部署但地址丢失。需向主人确认地址才能验证外网访问。